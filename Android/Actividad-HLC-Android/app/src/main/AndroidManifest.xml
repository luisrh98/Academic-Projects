<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:theme="@style/Theme.Aplicacion1"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Aplicacion1"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"

        tools:targetApi="31">
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Aplicacion1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.auth.LoginActivity"
            android:label="Login"
            android:theme="@style/Theme.Aplicacion1"/>

        <activity android:name=".ui.auth.RegisterActivity"
            android:label="Registro"
            android:theme="@style/Theme.Aplicacion1"/>

        <activity android:name=".ui.carrito.CarritoActivity"
            android:label="Carrito"
            android:theme="@style/Theme.Aplicacion1"/>

        <activity android:name=".ui.perfil.PerfilActivity"
            android:label="Perfil"
            android:theme="@style/Theme.Aplicacion1"/>

        <activity android:name=".ui.producto.ProductoDetalleActivity"
        android:label="Datalle productos"
            android:theme="@style/Theme.Aplicacion1"/>

        <activity android:name=".ui.qr.QrScannerActivity"
            android:label="QR"
            android:theme="@style/Theme.Aplicacion1"/>



    </application>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
</manifest>